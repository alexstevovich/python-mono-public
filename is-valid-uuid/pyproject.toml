[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"
[project]
name = "alexstevovich-is-valid-uuid"
version = "1.0.0"
description = "Validate RFC 9562 UUID strings and optional versions"
requires-python = ">=3.11"
dependencies = []
[tool.setuptools.packages.find]
where = ["src"]
