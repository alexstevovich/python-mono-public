[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "alexstevovich-tar-archive"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["cryptography>=50,<51"]

[tool.setuptools.packages.find]
where = ["src"]
