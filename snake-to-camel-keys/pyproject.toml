[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "alexstevovich-snake-to-camel-keys"
version = "1.0.0"
description = "Recursively convert mapping keys from snake case to camel case"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
