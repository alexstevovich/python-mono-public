[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "alexstevovich-media-download"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["yt-dlp>=2026.8.19,<2027"]

[tool.setuptools.packages.find]
where = ["src"]
