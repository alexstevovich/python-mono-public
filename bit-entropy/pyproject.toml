[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"
[project]
name = "alexstevovich-bit-entropy"
version = "1.0.0"
description = "Calculate fixed-length symbol-space entropy in bits"
requires-python = ">=3.11"
dependencies = []
[tool.setuptools.packages.find]
where = ["src"]
