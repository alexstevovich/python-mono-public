[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"
[project]
name = "alexstevovich-reduce-fraction"
version = "1.0.0"
description = "Reduce an integer fraction to a normalized string"
requires-python = ">=3.11"
dependencies = []
[tool.setuptools.packages.find]
where = ["src"]
