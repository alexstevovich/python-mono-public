[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "alexstevovich-image-convert"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["Pillow>=12,<13"]

[tool.setuptools.packages.find]
where = ["src"]
